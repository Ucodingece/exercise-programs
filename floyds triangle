def floyds_triangle(char):
    ch='A'
    for i in range(1,char+1):
        for j in range(1,i+1):
            print(ch, end=" ")
            ch=chr(ord(ch)+1)
        print( )
num_char=5
floyds_triangle(num_char)
